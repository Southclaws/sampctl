# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# goreleaser build space
dist/

# main binary
sampctl

# the one thing I hate about mac...
.DS_Store
